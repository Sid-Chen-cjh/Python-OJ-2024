



"""
import math as m

init = [0, 1]
while len(init) < pow(2, 10):
    temp = pow(2, int(m.log2(len(init))))
    ans = init
    for j in range(len(init)-1, -1, -1):
        ans.append(init[j] + temp)
    init = ans
print(ans)
"""


# n, k = map(int, input().split())

